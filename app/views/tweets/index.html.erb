<div id="main">
  <%= render partial: "tweets/dashboard" %>
  <div class="content-main" role="main">
    <div class="content-header">
      <div class="header-innner">
        <h2>Tweets</h2>
      </div>
    </div>
    <div class="content-tweet">
      <ul class="tweet-items">
        <% @tweets.each do |tweet|%>
        <li>
          <div class="content">
            <div class="header">
              <strong class="name">
                <%= tweet[:user][:name] %>
              </strong>
              <%= image_tag(tweet[:user][:image]) %> 
              <span class="date">
                <%=time_ago_in_words(tweet[:created_at]) %>
              </span>
            </div>
            <div class="tweet">
              <div class="name">
              <%= tweet[:text] %>
              </div>
            </div>
            <div class="extra">
            </div>
          </div>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
